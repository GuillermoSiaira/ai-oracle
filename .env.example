# Copiar como .env en la raíz del repo (mismo nivel que docker-compose.yml)
# Sin comillas, sin espacios alrededor de '='

# OpenAI
OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# Modelo por defecto (opcional). Ejemplos: gpt-4o-mini, gpt-4o
LILLY_MODEL=gpt-4o-mini

# Frontend (Next.js) URLs públicas
NEXT_PUBLIC_ABU_URL=http://localhost:8000
NEXT_PUBLIC_LILLY_URL=http://localhost:8001
